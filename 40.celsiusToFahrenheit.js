// Given celsius temperature as an integer, modify the convertO function so it converts the celsius temperature to Fahrenheit temperature

function convert(celsius) {
    return celsius * (9 / 5) + 32;
}